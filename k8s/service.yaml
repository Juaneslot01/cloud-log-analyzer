apiVersion: v1
kind: Service
metadata:
  name: log-analyzer-service
spec:
  selector:
    app: cloud-log-analyzer
  ports:
    - protocol: TCP
      port: 50051
      targetPort: 50051
  type: LoadBalancer
